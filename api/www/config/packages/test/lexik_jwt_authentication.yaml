lexik_jwt_authentication:
    # Use a simple secret string for testing
    encoder:
        service: lexik_jwt_authentication.encoder.lcobucci
        signature_algorithm: HS256
    token_ttl: 3600
    secret_key: 'test_secret_key_for_jwt_authentication'
