doctrine:
    dbal:
        # Use SQLite in-memory database for tests
        # Override the DATABASE_URL from .env.test
        url: 'sqlite:///:memory:'
    orm:
        auto_generate_proxy_classes: true
        enable_lazy_ghost_objects: true
        report_fields_where_declared: true
        validate_xml_mapping: true
